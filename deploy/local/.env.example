# Host-side bind port for local solver API.
# Use this URL in extension: http://127.0.0.1:<port>
SOLVER_API_BIND_PORT=18000

# Capture dataset options.
DATASET_CAPTURE_ENABLED=1
DATASET_CAPTURE_HOST_DIR=../../datasets
DATASET_CAPTURE_CONTAINER_DIR=/app/datasets
